{
  "name": "simple-lambda-trigger",
  "version": "1.0.0",
  "description": "simple-lambda-trigger",
  "author": "",
  "license": "MIT",
  "scripts": {
    "invoke:consumer" : "sls invoke local --function consumer",
    "invoke:scheduler" : "sls invoke local --function scheduler",
    "deploy": "sls deploy"
  },
  "devDependencies": {
    "serverless-offline": "^8.4.0",
    "serverless-offline-aws-eventbridge": "^1.6.6",
    "serverless-plugin-typescript": "^2.1.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "axios": "^0.26.0"
  }
}
